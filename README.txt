Open a file in the Term-paper/2/bin/Debug/2.exe
Zoom is using for zooming in (move to left) and out (move to right) of an object
Rotate by Y and X is rotating of coordinate axes
Show surface is for showing the border of every coordinate axes
Fill surface is for showing coordinate plane of every coordinate axes
Moving Figure is used for moving the square along the coordinate axis (x, y, z)
Figure Scaling is using for increasing (move to left) and decreasing (move to right)
First 4 tables from left in at the bottom of the application are for calculation processes
5th table show the coordinate of the square
